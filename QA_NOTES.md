# ملاحظات الاختبار

تم تحميل النسخة المحلية بنجاح، ونجح تسجيل دخول الأدمن بالحساب `rasim1010` وكلمة المرور `20203030` بعد إصلاح سجل الأدمن المتعارض في Firestore. ظهرت لوحة الأدمن بأقسام الرئيسية والحضور والمندوبين والإشعارات والموقع.

يعرض قسم الموقع خريطة Leaflet مع طبقة OpenStreetMap، وأزرار التكبير والتصغير، ومدخلات الإحداثيات، ومحدد النطاق. وتم بناء APK تجريبي بنجاح مع صلاحيات `ACCESS_FINE_LOCATION` و`ACCESS_COARSE_LOCATION`.

تم اختبار الموقع المنشور أيضًا: تحميل صفحة الدخول نجح، وتسجيل دخول الأدمن نجح، لكن الواجهة المنشورة ما زالت النسخة القديمة لأن GitHub رفض صلاحية الكتابة من جلسة التكامل الحالية؛ لذلك وصلت إصلاحات كلمة المرور إلى Firestore فقط، بينما تحتاج ملفات الواجهة إلى رفع يدوي أو منح صلاحية Contents: write للمستودع.

يجب إكمال الاختبار على أجهزة Android فعلية لأن دقة GPS وسلوك WebView لا يمكن التحقق منهما بالكامل داخل بيئة المحاكاة الحالية.

بعد إعادة تحميل النسخة المحلية الأخيرة، استمر تسجيل جلسة الأدمن وظهر قسم الموقع بالخريطة التفاعلية ومدخلات النطاق والإحداثيات دون أخطاء ظاهرة.

فحص APK النهائي أكد اسم الحزمة `com.deliveryattendance.app`، ونسخة Android المستهدفة 35، ووجود صلاحيات الإنترنت والموقع الدقيقة والتقريبية. لم توجد أجهزة Android فعلية متصلة عبر ADB في بيئة الاختبار، لذلك لم يُنفذ اختبار تثبيت وتشغيل متعدد الأجهزة فعليًا.

## اختبار الإصدار 2.0.0

تم بناء APK Release موقّع بنجاح، لكن أول تحميل للنسخة المحلية بقي مؤقتًا على شاشة التحميل دون ظهور خطأ في سجل المتصفح؛ جاري تشخيص مسار التهيئة والاتصال بقاعدة البيانات.

اكتمل تحميل الإصدار 2.0.0 بعد انتظار اتصال Firebase. نجح فتح قسم «الإدارة»، وظهرت واجهة الإجازات وسجل التدقيق وفحص التحديث مع رقم الإصدار 2.0.0 دون أخطاء ظاهرة.

نجح فتح قسم الحضور، وظهرت فلاتر الاسم والتاريخ والحالة، وأزرار تصدير Excel وطباعة التقرير. لم يسجل المتصفح أخطاء JavaScript بعد تحميل القسم.

تم فتح مشروع Firebase الصحيح عبر المتصفح الشخصي المسجل بحساب مالك المشروع. صفحة Authentication متاحة، لكن زر البدء لم ينتقل بعد إلى إعداد المزود ويحتاج إعادة محاولة بعد اكتمال تحميل لوحة Firebase.

تم الوصول إلى صفحة مزود Email/Password داخل Firebase Authentication، وأصبحت شاشة التفعيل جاهزة للحفظ.

تم تفعيل مزود Email/Password بنجاح في Firebase Authentication، وظهرت حالته Enabled في لوحة المشروع.

تم الوصول إلى صفحة Firebase Admin SDK وظهر زر إنشاء مفتاح خاص جديد لحساب الخدمة `firebase-adminsdk-fbsvc@delivery-attendance-20663.iam.gserviceaccount.com`.

تم تأكيد إنشاء مفتاح Firebase Admin الخاص وتنزيله من لوحة المشروع بعد موافقة المستخدم.

تعذر على المتصفح الشخصي الوصول مباشرة إلى localhost، لذلك تم فتح الخادم المحلي عبر رابط HTTPS مؤقت. ظهرت شاشة الدخول الجديدة بنجاح وأصبحت جاهزة لاختبار Firebase Authentication والخدمات الخادمية.

تم تجهيز نموذج دخول الأدمن على النسخة الكاملة ببيانات الحساب لاختبار مسار Firebase Authentication ثم جلسة الخادم.

نجح تسجيل دخول الأدمن بالحساب `rasim1010` عبر Firebase Authentication، ونجحت خدمة الجلسة الخادمية، ثم انتقلت الواجهة إلى لوحة التحكم دون خطأ.

تم الوصول إلى لوحة Vercel بحساب المستخدم وظهر مشروع `delivery-attendance`. محاولة فتح بطاقة المشروع الأولى تزامنت مع تحديث الصفحة، لذلك لم تُنفذ وتحتاج إعادة المحاولة بعد تحديث العرض.

تم فتح مشروع `delivery-attendance` داخل حساب Vercel المرتبط بالموقع، وأصبح الانتقال إلى إعدادات المشروع والمتغيرات السرية متاحًا.

تم الوصول إلى صفحة Environment Variables في مشروع Vercel، ولا توجد متغيرات حالية. ظهر خيار إضافة متغير بيئة جديد لتخزين مفتاح Firebase بأمان.

تم فتح نموذج إضافة متغير Secret في Vercel. خيار استيراد `.env` لم يُظهر مدخل رفع ملف في الواجهة، لذا سيتطلب إدخال المفتاح عبر الحقول أو مسار نشر بديل.

تم تشغيل Vercel CLI وفتح صفحة تفويض الجهاز في حساب Vercel الصحيح. ظهر طلب Allow Access للربط الذي بدأ من بيئة البناء.

تم نشر الإصدار 2.0.0 بنجاح إلى Vercel وربطه بالنطاق الإنتاجي `https://delivery-attendance.vercel.app/`. ظهرت شاشة الدخول الجديدة من الموقع المنشور، وتمت إضافة مفتاح Firebase Admin كمتغير Secret للإنتاج.

تعذر نشر قواعد Firestore من CLI لأن حساب الخدمة لا يملك صلاحية فحص Service Usage. تم الانتقال إلى قسم Firestore في لوحة Firebase بحساب المالك لإكمال نشر القواعد يدويًا.

تم تحميل Firestore بنجاح، وظهر تبويب Rules. كما تأكد وجود سجل تدقيق لتسجيل دخول الأدمن من اختبار الإنتاج.

محرر Firestore أظهر أن القواعد الحالية ما زالت عامة `allow read, write: if true`. سيتم استبدالها بالقواعد المقيدة التي تمنع الكتابة المباشرة للحضور والحسابات الحساسة.

المحرر لم يعكس الإدخال الأول عبر الحقل الملتقط، وبقيت القواعد العامة ظاهرة. سيتم تحديد النص داخل محرر Firebase بلوحة المفاتيح ثم إدخال القواعد مجددًا.

نجح إدخال القواعد المقيدة في محرر Firebase، وظهرت حالة `unpublished changes` مع زر Publish. القواعد لم تُنشر بعد.

تم نشر الإصدار النهائي 2.0.0 على Vercel بعد تحديد الفريق صراحة. تحقق الإنتاج من `version.json` بإصدار 2.0.0 ورابط APK المباشر، ونجح تسجيل دخول الأدمن عبر `/api/login` بعد نشر قواعد Firestore.

بدأ اختبار الواجهة الإنتاجية بعد نشر القواعد المقيدة؛ ظهر نموذج الدخول وتم إدخال اسم مستخدم الأدمن بنجاح.

تم إدخال بيانات الأدمن وإرسال النموذج من الموقع الإنتاجي بعد نشر القواعد المقيدة؛ الواجهة بدأت معالجة الطلب.

نجح دخول الأدمن بعد نشر القواعد المقيدة. قسم الحضور يعمل ويعرض فلتر اسم المندوب، التاريخ من/إلى، الحالة، وتصدير Excel والطباعة، مع عدم وجود سجلات حاليًا.

قسم المندوبين يعمل ويعرض حالات نشط/بانتظار الموافقة والبحث. قسم الموقع يعمل وتظهر خريطة Leaflet تفاعلية، اختيار الموقع، إدخال الإحداثيات، وزيادة النطاق من 25 إلى 500 متر مع القيمة الحالية 100 متر.

قسم الإدارة يعمل ويعرض الإجازات والملاحظات، سجل التدقيق، الإصدار الحالي 2.0.0 وفحص التحديث. قسم الإشعارات يعمل ويعرض تنبيهات الجهاز الثاني فقط، ولا توجد إشعارات حالية.

تم بناء `delivery-attendance-v2.apk` بإصدار Release موقّع بنجاح، وتحقق apksigner من توقيع v1 وv2 ووجود موقّع واحد. SHA256 للـAPK: `a1a6cbb2d8c549d3f7025033cb02b6d3c110cc500a8fdebfc40ddd5fda8f17f0`.
تم إنشاء `delivery-attendance-source.zip` مع استبعاد مفاتيح Firebase والتوقيع وكلمات المرور وملفات البناء. SHA256 للحزمة: `9f1cfeadeebeb070972137f9cf029bc3066ac1146421e0ccc785a30ced6ca1b8`.

بعد نشر الإصدار 2.0.1 وإنشاء حساب اختبار، ظهر في لوحة الأدمن عداد `1 حساب بانتظار الموافقة` فور فتح قسم المندوبين. هذا يؤكد أن مسار التسجيل وقائمة الموافقة يعملان.

تشخيص البلاغ الأخير: إنشاء الحساب كان يعمل في API، لكن لوحة الأدمن كانت تقرأ users مرة واحدة عند فتحها؛ لذلك الحساب الجديد لا يظهر إذا أُنشئ بعد فتح اللوحة. الإصلاح في 2.0.1 أضاف onSnapshot لقائمة users وإعادة تحميل عند فتح قسم المندوبين.
تم اختبار حساب QA: ظهر عداد بانتظار الموافقة وبطاقة الحساب، ثم تمت الموافقة عليه من الأدمن. بعد الموافقة نجح Firebase Auth ودخلت جلسة agent بحالة active عبر `/api/session`. محاولة الحضور وصلت إلى `/api/attendance` وتم رفضها فقط لأن موقع المخزن/الإحداثيات الفعلية لا تطابق موقع الاختبار، ما يؤكد وصول المسار الصحيح إلى خدمة الحضور.
تم تنظيف حساب QA وبياناته بعد الاختبار. APK الإصدار 2.0.1 موقّع، SHA256: `359c6ccb5f646b006caa09aef1dc07662366cac999b46243e64c141d7ef7186d`. حزمة المصدر الجديدة SHA256: `a88a40c05fb1455e1b62dd2d25435f43e417b965afcfb7646d779a263754dca9`.

تصحيح رقم النسخة: كان `versionName` داخل Android مضبوطًا على 2.0.0 رغم أن ملف `version.json` كان 2.0.1. تم تعديل Gradle إلى `versionCode 21` و`versionName 2.0.1`، وإعادة بناء APK Release موقّع. تحقق APK المنشور عبر aapt: `com.deliveryattendance.app versionCode='21' versionName='2.0.1'`. تحقق `version.json` المنشور أيضًا 2.0.1.

إصدار 2.0.2: تم إخفاء بطاقة تحديث APK من الويب عبر IS_NATIVE، وتحديث APP_VERSION داخل الواجهة، وإجبار مسار الدخول على قراءة role الحقيقي من ملف users بدل افتراض agent. تحقق الإنتاج من version.json وAPK: versionCode 22 وversionName 2.0.2.
