# ملاحظات الاختبار

تم تحميل النسخة المحلية بنجاح، ونجح تسجيل دخول الأدمن بالحساب `rasim1010` وكلمة المرور `20203030` بعد إصلاح سجل الأدمن المتعارض في Firestore. ظهرت لوحة الأدمن بأقسام الرئيسية والحضور والمندوبين والإشعارات والموقع.

يعرض قسم الموقع خريطة Leaflet مع طبقة OpenStreetMap، وأزرار التكبير والتصغير، ومدخلات الإحداثيات، ومحدد النطاق. وتم بناء APK تجريبي بنجاح مع صلاحيات `ACCESS_FINE_LOCATION` و`ACCESS_COARSE_LOCATION`.

تم اختبار الموقع المنشور أيضًا: تحميل صفحة الدخول نجح، وتسجيل دخول الأدمن نجح، لكن الواجهة المنشورة ما زالت النسخة القديمة لأن GitHub رفض صلاحية الكتابة من جلسة التكامل الحالية؛ لذلك وصلت إصلاحات كلمة المرور إلى Firestore فقط، بينما تحتاج ملفات الواجهة إلى رفع يدوي أو منح صلاحية Contents: write للمستودع.

يجب إكمال الاختبار على أجهزة Android فعلية لأن دقة GPS وسلوك WebView لا يمكن التحقق منهما بالكامل داخل بيئة المحاكاة الحالية.

بعد إعادة تحميل النسخة المحلية الأخيرة، استمر تسجيل جلسة الأدمن وظهر قسم الموقع بالخريطة التفاعلية ومدخلات النطاق والإحداثيات دون أخطاء ظاهرة.

فحص APK النهائي أكد اسم الحزمة `com.deliveryattendance.app`، ونسخة Android المستهدفة 35، ووجود صلاحيات الإنترنت والموقع الدقيقة والتقريبية. لم توجد أجهزة Android فعلية متصلة عبر ADB في بيئة الاختبار، لذلك لم يُنفذ اختبار تثبيت وتشغيل متعدد الأجهزة فعليًا.
